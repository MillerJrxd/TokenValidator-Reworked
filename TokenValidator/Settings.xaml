<Window x:Class="TokenValidator.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Properties="clr-namespace:TokenValidator.Properties"
        xmlns:local="clr-namespace:TokenValidator"
        mc:Ignorable="d"
        Title="Settings" 
        Height="450" Width="800"
        MinHeight="450" MinWidth="800"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E"
        WindowStyle="None"
        AllowsTransparency="True"
        BorderThickness="1"
        BorderBrush="#333333">
    <Window.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#444444"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#555555"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#F1707A"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SettingsButton" TargetType="Button">
            <Setter Property="Background" Value="#3E3E40"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#555555"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SettingsTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="CaretBrush" Value="#007ACC"/>
            <Setter Property="SelectionBrush" Value="#007ACC"/>
            <Setter Property="SelectionTextBrush" Value="White"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#007ACC"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="False">
                                <Setter Property="Background" Value="#2D2D30"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Custom Title Bar -->
        <Grid Height="38" VerticalAlignment="Top" Background="#252526">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- App Icon and Title -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="12,0,0,0">
                <Image Source="pack://application:,,,/TokenValidator;component/Resources/icon.ico" 
                       Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock Text="Settings" FontWeight="SemiBold" FontSize="14" 
                           Foreground="#DDDDDD" VerticalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect ShadowDepth="1" Direction="330" Color="Black" Opacity="0.6" BlurRadius="2"/>
                    </TextBlock.Effect>
                </TextBlock>
            </StackPanel>

            <!-- Draggable area -->
            <Grid Grid.Column="0" Grid.ColumnSpan="2" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"/>

            <!-- Window Controls -->
            <Button x:Name="MinimizeButton" Grid.Column="2" Style="{StaticResource TitleBarButtonStyle}" Content="&#xE949;" Click="MinimizeButton_Click" ToolTip="Minimize"/>
            <Button x:Name="CloseButton" Grid.Column="3" Style="{StaticResource CloseButtonStyle}" Content="&#xE8BB;" Click="CloseButton_Click" ToolTip="Close"/>
        </Grid>

        <!-- Main Content -->
        <Grid Margin="15,38,15,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Settings Title -->
            <TextBlock Grid.Row="0" Text="Application Settings" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

            <!-- Settings Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Hotkey Settings -->
                    <Border Background="#2D2D30" CornerRadius="4" Padding="15">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Text="Hotkey Settings" FontWeight="Bold" FontSize="16" Foreground="White" Margin="0,0,0,10"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="QR Scan Hotkey:" VerticalAlignment="Center" Foreground="White" Margin="0,0,10,0"/>
                            <TextBox x:Name="hotkeyTextBox" Grid.Row="1" Grid.Column="1" Style="{StaticResource SettingsTextBox}" 
                                     PreviewKeyDown="HotkeyTextBox_PreviewKeyDown" PreviewMouseDown="HotkeyTextBox_PreviewMouseDown">
                                <TextBox.ToolTip>
                                    <ToolTip Content="Current hotkey combination for QR scanning" 
                                              Background="#252526" Foreground="White"
                                              BorderBrush="#555555"/>
                                </TextBox.ToolTip>
                            </TextBox>
                            <Button x:Name="recordHotkeyButton" Grid.Row="1" Grid.Column="2" Content="Record" Style="{StaticResource SettingsButton}" Click="RecordHotkeyButton_Click" Background="#007ACC" Height="30" Width="60"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Text="Press the Record button and then press your desired key combination (keyboard or mouse)" 
                                       FontStyle="Italic" Foreground="#AAAAAA" Margin="0,10,0,0" TextWrapping="Wrap"/>
                        </Grid>
                    </Border>

                    <Border Background="#2D2D30" CornerRadius="4" Padding="15" Margin="0,15,0,0">
                        <StackPanel>
                            <TextBlock Text="Seasonal Effects" FontWeight="Bold" FontSize="16" Foreground="White" Margin="0,0,0,10"/>
                            <CheckBox x:Name="seasonalEffectsCheckBox" Content="Enable seasonal visual effects" 
                  Foreground="White" FontSize="14" IsChecked="{Binding Source={x:Static Properties:Settings.Default}, Path=SeasonalEffects, Mode=TwoWay}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Save/Cancel Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button x:Name="cancelButton" Content="Cancel" Style="{StaticResource SettingsButton}" Click="CancelButton_Click" Margin="0,0,10,0" Width="60" Height="30"/>
                <Button x:Name="saveButton" Content="Save Settings" Style="{StaticResource SettingsButton}" Click="SaveButton_Click" Background="#007ACC" Height="30" Width="90"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
