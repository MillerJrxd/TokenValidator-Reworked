<Window x:Class="TokenValidator.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        MinWidth="340"
        MaxWidth="520"
        Topmost="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/AppStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid x:Name="ContentContainer">
        <Border Background="{DynamicResource MaterialDesignPaper}"
            BorderBrush="{DynamicResource MaterialDesignDivider}"
            BorderThickness="1"
            CornerRadius="10"
            Padding="0">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="32" ShadowDepth="6" Opacity="0.6" Direction="270"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title Bar -->
                <Border Grid.Row="0" Background="{DynamicResource MaterialDesignToolBarBackground}"
                    CornerRadius="10,10,0,0" Padding="16,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <materialDesign:PackIcon x:Name="TitleIcon" Grid.Column="0"
                                            Kind="Information" Width="20" Height="20"
                                            VerticalAlignment="Center"
                                            Margin="0,0,10,0"
                                            Foreground="{DynamicResource MaterialDesignBody}"/>

                        <TextBlock x:Name="TitleText" Grid.Column="1"
                               FontWeight="SemiBold" FontSize="14"
                               Foreground="{DynamicResource MaterialDesignBody}"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis">
                            <TextBlock.Effect>
                                <DropShadowEffect ShadowDepth="1" Direction="330" Color="Black" Opacity="0.5" BlurRadius="2"/>
                            </TextBlock.Effect>
                        </TextBlock>

                        <Button Grid.Column="2" Style="{StaticResource CloseButtonStyle}"
                            Width="32" Height="32" Margin="8,0,0,0"
                            Click="CloseButton_Click" ToolTip="Close">
                            <materialDesign:PackIcon Kind="Close" Width="14" Height="14"/>
                        </Button>
                    </Grid>
                </Border>

                <!-- Content Area -->
                <Grid Grid.Row="1" Margin="24,20,24,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Icon -->
                    <Border Grid.Column="0" x:Name="IconBorder"
                        Width="48" Height="48"
                        CornerRadius="24"
                        Margin="0,0,18,0"
                        VerticalAlignment="Top">
                        <materialDesign:PackIcon x:Name="ContentIcon"
                                            Kind="Information"
                                            Width="26" Height="26"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                    </Border>

                    <!-- Message -->
                    <TextBlock Grid.Column="1" x:Name="MessageText"
                           FontSize="14" LineHeight="22"
                           Foreground="{DynamicResource MaterialDesignBody}"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           MaxWidth="380"/>
                </Grid>

                <!-- Separator -->
                <Separator Grid.Row="2" Margin="0" 
                       Background="{DynamicResource MaterialDesignDivider}"
                       Height="1" VerticalAlignment="Top"/>

                <!-- Button Area -->
                <StackPanel Grid.Row="2" x:Name="ButtonPanel"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="24,12,24,16"/>
            </Grid>
        </Border>
    </Grid>
</Window>
